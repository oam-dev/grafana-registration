apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: grafana-datasource-registration-role
subjects:
- kind: ServiceAccount
  name: default
  namespace: {{ .Release.Namespace }}
